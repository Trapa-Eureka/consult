# Claude API key — without it, falls back to rule-based mock triage
ANTHROPIC_API_KEY=

# MongoDB — without it, falls back to in-memory dummy data
MONGODB_URI=mongodb://localhost:27017/konsulta

PORT=4000
# Frontend origin (CORS)
CLIENT_ORIGIN=http://localhost:5173
